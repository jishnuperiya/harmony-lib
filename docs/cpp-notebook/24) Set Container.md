
# What is std::set

- `std::set<T>` is an **ordered container of unique elements**.

✔ no duplicates  
✔ automatically sorted  
✔ fast lookup (log n)  

Internally, it is a **balanced red–black tree**.

